import SetRepository from './SetRepositoryPostgre';

const makeSut = () => {
	const setRepository = new SetRepository();
	return { setRepository };
};

describe('SetRepository', () => {
	describe('create', () => {
		beforeEach(() => {});
		it('should ', () => {});
	});
});
