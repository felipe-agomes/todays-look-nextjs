import Clothe from './Tables/Clothe';
import sequelize from './connection';

describe('ClotheRepository', () => {
	it('should ', async () => {
		// const clothe = Clothe.create({
		// 	category: 'a',
		// 	key: 'a',
		// 	image: 'a',
		// 	userId: '1',
		// });
		// await sequelize.sync({ force: true });
	});
});
